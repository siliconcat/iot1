storm.zookeeper.servers:
  - "zoo-1.silicon.cat"

storm.zookeeper.port: 2181

storm.local.dir: "storm-local"

java.library.path: "/usr/local/lib:/opt/local/lib:/usr/lib"

nimbus.host: "storm-master.silicon.cat"

supervisor.slots.ports:
  – 6700
  – 6701
  – 6702
  – 6703